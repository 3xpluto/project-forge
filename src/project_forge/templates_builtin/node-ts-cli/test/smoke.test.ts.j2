import { describe, it, expect } from "vitest";
import { greet } from "../src/index";

describe("greet", () => {
  it("formats greeting", () => {
    expect(greet("world")).toBe("hello, world");
  });
});
