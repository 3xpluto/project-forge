# Node templates

All Node templates are **TypeScript** and ship with `vitest`.

## node-ts-cli
A minimal CLI using `yargs` and `tsx` for dev runs.

## node-express-api
Express service with `/health` and tests using `supertest`.
