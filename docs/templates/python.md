# Python templates

## python-cli
A minimal CLI using stdlib `argparse`.

## python-fastapi
FastAPI service with `/health` endpoint.


## python-fastapi-postgres
FastAPI service with SQLAlchemy and a Postgres-ready `DATABASE_URL` (defaults to SQLite).
