# Style guide

- Prefer small templates; move optional tooling into features.
- Keep manifests stable; bump schema only when necessary.
- Never allow path traversal in destination paths.
- Provide a runnable demo + smoke tests for each template.
